<div class="max-w-2xl mx-auto px-6 lg:px-8 py-16">
  <!-- Header -->
  <div class="mb-12">
    <nav class="mb-6">
      <%= link_to seller_products_path, class: "text-sm text-neutral-400 hover:text-neutral-900 flex items-center gap-2" do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
        Back to products
      <% end %>
    </nav>
    <h1 class="text-3xl font-light">New Product</h1>
    <p class="text-sm text-neutral-500 mt-2">Add a new digital product to your store.</p>
  </div>

  <%= render "form" %>
</div>
